<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<?xml-stylesheet href="./_c74_ref.xsl" type="text/xsl"?>

<!--MEMO-->
<!-- <m> initwith </m> : references a message -->
<!-- <o> loadbang </o> : references an object -->
<!-- <i> mvc.overview.maxpat </i> : references a patcher -->
<!-- <at>default</at> : references an attribute -->
<!-- <link type="vignette" module="aim" name="brace_expansion">brace expansion notation</link> : add a link to a vignette -->
<!-- <img src="images/image137.png" /> : put an image -->

<c74object name="mvc.message.remote" module="aim" kind="patcher" category="library">

	<digest>
		Monitor and remote control values from an <o>mvc.message</o>.
	</digest>
	
	<description>
		<o>mvc.message.remote</o> allows to bind to an <o>mvc.message</o>  for remote control or monitoring.
		The <o>mvc.message</o> it refers to can be set dynamically and multiple addresses can be provided through <link type="vignette" module="aim" name="brace_expansion">brace expansion notation</link>.<br />

		<o>mvc.message.remote</o> helps implementing the <i>view</i> and <i>controller</i> part in the <link type="vignette" module="aim" name="MVC_principles">MVC model</link> used in the MVC package.
	</description>
	
	<!--METADATA-->
	<metadatalist>
		<metadata name="author">Vincent Goudard</metadata>
		<metadata name="tag">MVC</metadata>
		<metadata name="tag">mapping</metadata>
		<metadata name="aim-type">core</metadata>
	</metadatalist>
	
	<!--INLETS-->
	<inletlist>
		<inlet id="0" type="anything">
			<digest>Messages to be sent (see below)</digest>
		</inlet>
		<inlet id="1" type="anything">
			<digest>Settings for the <o>mvc.message.remote</o> (see below)</digest>
		</inlet>
	</inletlist>

	<!--OUTLETS-->
	<outletlist>
		<outlet id="0" type="anything">
			<digest>Monitored <o>mvc.message</o> values (see below)</digest>
		</outlet>
		<outlet id="1" type="list" size="1">
			<digest>Monitored <o>mvc.message</o> address with index in the <o>mvc.message.remote</o> namespace (see below)</digest>
		</outlet>
		<outlet id="2" type="anything">
			<digest>Settings dumpout  (see below)</digest>
		</outlet>
	</outletlist>
	
	<!--ARGUMENTS-->
	<objarglist>
		<objarg name="message-address" optional="0" type="symbol">
			<digest>
				Monitored <o>mvc.message</o> adress(es).
			</digest>
			<description>
				If a single argument is provided, the <m>address</m> of the monitored <o>mvc.message</o> should be expressed with slash-separated symbols (à la OSC). The last item in this OSC string will be considered as the <m>name</m> of the <o>mvc.message</o> and everything that is before the last slash will be considered as the <m>path</m> of the address.

				If no slash is present in the address, the symbol will be simply be considered as a <o>mvc.message</o> name and will wait for a path to this name to initialize.
			</description>
		</objarg>
		<objarg name="message-address" optional="1" type="symbol">
			<digest>
				Monitored <o>mvc.message</o> adress(es).
			</digest>
			<description>
				If two arguments are provided, the first argument will be considered as the <m>path</m> of the address and the second argument as the <m>name</m> of the <o>mvc.message</o>. The resulting <m>address</m> will be the concatenation of the <m>path</m> and the <m>name</m>.
			</description>
		</objarg>
	</objarglist>
	
	<!--MESSAGES-->
	<methodlist>
		<method name="anything">
			<arglist>
				<arg name="values" optional="0" type="list"/>
			</arglist>
			<digest>
				Feed values to <o>mvc.message.remote</o> (on the left inlet).
			</digest>
			<description>
				The values fed to the left inlet will be sent to all addresses covered by the <o>mvc.message.remote</o> namespace, given an matching <o>mvc.message</o> address exists.
			</description>
		</method>

		<method name="setvalue">
			<arglist>
				<arg name="index" optional="0" type="int"/>
				<arg name="values" optional="0" type="list"/>
			</arglist>
			<digest>
				Set the value of a specific <m>address</m> in the <o>mvc.message.remote</o> namespace (on the left inlet).
			</digest>
			<description>
				<m>setvalue</m> is a possible method on the left inlet.<br />

				The values will be sent to the addresses of the <o>mvc.message.remote</o> at the specified index (following the setvalue method in MC components).
			</description>
		</method>

		<method name="name">
			<arglist>
				<arg name="message name" type="symbol" optional="0"/>
			</arglist>
			<digest>
				Set the target <o>mvc.message</o> <m>name</m> (on the right inlet).
			</digest>
			<description>
				<m>name</m> is a possible method on the right inlet.<br />

				A full <o>mvc.message</o> <m>address</m> is made up by concatenating a <m>path</m> and a <m>name</m>.
				Multiple <o>mvc.message</o> addresses can be set with the <link type="vignette" module="aim" name="brace_expansion">brace expansion notation</link>.
			</description>
		</method> 

		<method name="path">
			<arglist>
				<arg name="path" type="symbol" optional="0"/>
			</arglist>
			<digest>
				Set the path of the target <o>mvc.message</o> (on the right inlet).
			</digest>
			<description>
				<m>path</m> is a possible method on the right inlet.<br />

				A full <o>mvc.message</o> <m>address</m> is made up by concatenating a <m>path</m> and a <m>name</m>.
				Multiple <o>mvc.message</o> addresses can be set with the <link type="vignette" module="aim" name="brace_expansion">brace expansion notation</link>.
			</description>
		</method>

		<method name="address">
			<arglist>
				<arg name="address" type="symbol" optional="0"/>
			</arglist>
			<digest>
				Sets the value of a specific address in the <o>mvc.message.remote</o> namespace (on the right inlet).
			</digest>
			<description>
				<m>address</m> is a possible method on the right inlet.<br />

				The address of a specific <o>mvc.message</o> can be given as an OSC formatted symbol. A full <o>mvc.message</o> <m>address</m> is considered to be the concatenation of a <m>path</m> and a <m>name</m>.<br />
				When setting a full <m>address</m>, the <o>mvc.message.remote</o> will parse it and implicitely set the <m>name</m> as the last part of the address and the <m>path</m> as everything that comes before this last part.<br />
				Multiple <o>mvc.message</o> addresses can be set with the <link type="vignette" module="aim" name="brace_expansion">brace expansion notation</link>.
			</description>
		</method>

	</methodlist>
	
	<!--SEEALSO-->
	<seealsolist>
		<seealso name="mvc.model"/>
		<seealso name="mvc.device"/>
		<seealso name="mvc.parameter"/>
		<seealso name="mvc.remote"/>
		<seealso name="mvc.state"/>
		<seealso name="mvc.state.remote"/>
		<seealso name="mvc.message"/>
		<seealso name="mvc.gui.number"/>
	</seealsolist>
	
</c74object>